$("#follow_form").html("<%= escape_javascript(render('users/follow')) %>");
$("#followers").html('<%= escape_javascript(link_to pluralize(@user.followers.count,
  t("page.users.relationships.number_followers")),
  relationship_path(@user, query:"followers")) %>');
